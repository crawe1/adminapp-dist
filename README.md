# AdminApp — dystrybucja (auto-generowane, nie edytuj)

Publiczne repo z gotowym buildem. Zawiera tylko:
- `AdminApp.exe` — najnowsza wersja aplikacji,
- `version.txt` — numer wersji (używany do sprawdzania aktualizacji).

Kod źródłowy jest w osobnym, prywatnym repozytorium. Zawartość tej
gałęzi jest nadpisywana automatycznie przez CI przy każdym wydaniu.
